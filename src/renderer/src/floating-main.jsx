import React from 'react';
import ReactDOM from 'react-dom/client';
import { Floating } from './workagotchi/Floating';
import './floating.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Floating />
  </React.StrictMode>
);
